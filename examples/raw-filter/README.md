# Raw Filter Example

Uses the `filter` field to pass a custom OData expression directly to the auto-assignment policy.
Use this when the structured fields (dept_code, dept_name, include/exclude_title_prefixes) do not cover your attribute set.
