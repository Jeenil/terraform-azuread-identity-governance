# Cross-Department Example

One access package auto-assigned across multiple departments using a raw OData filter.
Use this when resources are genuinely shared between departments rather than owned by one.
