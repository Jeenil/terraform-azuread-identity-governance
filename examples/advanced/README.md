# Advanced Example

Member and owner split across two packages sharing the same resources (groups, Teams, SharePoint).
Auto-assignment policies use structured fields to build the OData filter automatically.
